export interface education {
  titulo: string,
  fechaInicio: Date | null,
  fechaFinal: Date | null,
  ubicacion: string
}

export interface experiencia {
  titulo: string,
  fechaInicio: Date | null,
  fechaFinal: Date | null,
  descrip: string
}
